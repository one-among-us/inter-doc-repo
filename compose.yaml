services:
  interdocrep:
    build: .
    container_name: oau-interdocrep
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./config.toml:/app/config.toml
      - ./service_account.json:/app/service_account.json
